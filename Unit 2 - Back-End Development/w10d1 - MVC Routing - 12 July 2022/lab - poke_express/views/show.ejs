<!DOCTYPE html>

    <head>
        <title>Pokemon</title>
    </head>

    <body>

        <% if (errMsg !== '') { %>
            <div class="error">
                <p>You have an error: <%= errMsg %> </p>
            </div>
        <% } else { %> 
        
            <h1><%= p.name.charAt(0).toUpperCase() + p.name.slice(1) %></h1>

            <figure>
                <img src="<%= p.img %>.jpg" />
            </figure>

        <% } %> 

    </body>

</html>